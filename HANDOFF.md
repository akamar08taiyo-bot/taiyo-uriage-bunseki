# 売上分析ダッシュボード — ステップ5（裏側実装）完了記録

**2026-07-27 完了。** 引き継ぎ指示書の全項目を実装し、実CSV（4,558行）でQAチェックリストを全項目クリア済み。
配布物は `index.html`（単一HTML・320KB・CDN依存ゼロ）。営業所へはこのファイル1個のコピーで配布できる。

## 実装済みの全体像

- ストレージ: `taiyo_uriage_rows_v1` / `taiyo_uriage_masters_v1` / `taiyo_uriage_meta_v1`（localStorage）
- 取込: CSV D&D／クリック選択／Excel貼り付け(タブ区切り)。文字コード自動判別(UTF-8→Shift-JIS)、
  引用符対応パーサー、列名マッピング（位置不問）、行検証（行番号付き報告）、先頭5行プレビュー確認モーダル
- 蓄積型・重複スキップ: キー=売上番号+商品名+数量+金額。**同一キーの正当な複数明細に対応するため
  「保存済みの出現回数まで」スキップする方式**（実CSVに同一キーの別明細が93行あったための設計判断。
  初回取込4,558行全行追加・再取込全行スキップの両立を確認済み）
- 集計: 1パス索引(buildAnalytics)＋useMemo。基準月=データ内最新月（ヘッダーのセレクタで変更可）
- 施設/個人自動判定: 強/弱キーワード＋「姓␣名」形。実CSVで施設75/個人303/要確認59。設定画面で移動可
- 減少アラート: 基準月 vs 直近3ヶ月平均。**3ヶ月平均が月¥3,000未満の施設は除外**（ノイズ対策、
  settings.alertMinBase）。発注漏れ: 直近4ヶ月中3ヶ月以上購入×基準月ゼロ
- 印刷3帳票（月次レポート/販売実績リスト/発注書）: 実データ配線・発行日/発注番号は当日から動的生成
- CSV出力(UTF-8+BOM・Excel対応)/JSONエクスポート・インポート(置き換え/統合)/全削除(二段階確認)

## 方針変更（2026-07-28・ユーザー指示）

**アラート機能は廃止し、分析と帳票作成のアプリにした。**

1. **減少アラート・発注漏れリストを全削除**（集計・ダッシュボード・月次レポート・サイドバーのバッジすべて）
   - ダッシュボードは「施設別売上TOP10」＋「商品別売上TOP10」のランキング2枚に置換
   - 月次レポート印刷の2章・3章は「分類別売上」「商品別売上TOP10」に差し替え
   - 施設別の増減列・担当別のアラート列は DeltaPill（前月比）に変更
2. **施設別の分析を深掘り**（行を展開すると表示）
   - 指標7つ: 今月売上／月平均／年間累計／粗利率／注文回数(回月)／最終購入日／返品件数
   - 月別売上推移を AreaChart で大きく表示（従来はミニ棒グラフ）
   - 商品別売上ランキングTOP10・販売個数ランキングTOP10（各バー付き）
   - 分類別の購入構成（構成比バー）・最近の購入明細10件
   - 「販売リストを印刷」「発注書を作成」「見積書を作成」の3ボタン
3. **商品分析に「ランキング」サブタブ追加**（売上金額TOP20 / 販売個数TOP20・直近12ヶ月）
4. **発注書の意味を変更**: 当社→仕入先 ではなく **施設→当社**（施設が当社へ出す注文書を担当営業が代理作成）。
   宛先=太陽シルバーサービス株式会社 御中、発注元ブロック=施設名＋担当者印欄。適正在庫画面の
   仕入先向け発注書機能・チェックボックス・単価記憶（priceOverrides）は削除
5. **タブを2つ新設**: 「発注書作成」「見積書作成」（DocComposer で共通実装）
   - 商品マスタは販売履歴から自動生成。「この施設の購入品」/「全商品」切替、種別(分類)プルダウン、商品名検索
   - 直近販売価格を初期単価に（その施設への価格を優先、なければ全体の最新価格。出所を「この施設」/「全体」で表示）
   - 金額の記載はトグル制。**発注書=初期OFF（数量のみ）／見積書=初期ON**
   - 見積書は taiyo-mitsumori 準拠（御見積金額枠・見積番号・有効期限30日・小計/消費税10%/合計・支払条件）
   - 作成中の明細はタブを移動しても保持（App の orderDraft / quoteDraft）

## 分類・営業所・帳票の強化（2026-07-28 第2次指示）

1. **分類に「紙製品」「部材」を追加**（全11分類）。内蔵辞書も更新
   - オムツ・パッド: 「テープ止」「いちばん」等を追加 →「エルモアいちばん すっきり伸縮テープ止Ｍ」が正しく入る
   - 紙製品: トイレット/ティシュ/ペーパー/シングル/ダブル/芯なし 等（清拭・入浴の「タオル」より前に評価）
   - 部材: 丸棒/ブラケット/ベースプレート/インテリアバー/アッシュ/ディンプル 等
2. **キーワード学習ルール**（masters.catRules = [{kw, cat}]）を新設。設定→商品分類 で登録・削除でき、
   登録時に該当件数をプレビュー。**次回以降の取込で入る新商品にも自動適用される**
   - 分類の優先順位: ①手動(catOverrides) ②学習(catRules) ③内蔵辞書。判定のもとを「手動/学習/自動」バッジで全画面に表示
   - 変更は 適正在庫の行展開 と 設定→商品分類 の両方から可能
3. **在庫扱いのデフォルトを「受注発注」に変更**（HANDLING_DEFAULT='order'。以前は在庫品）
   - 適正在庫を開くと分類作業を促すバナー（在庫品0件なら警告色）＋進捗「分類済み X/Y件」を表示
   - 一覧に「扱い」列（3ボタン即時切替）と分類フィルタを追加。絞り込み中の全件を一括変更するボタンも用意
4. **営業所マスタ19拠点を同梱**。初回起動時に営業所選択画面（OfficePicker）を出し、
   masters.settings.office に記憶して以後は表示しない。設定→営業所 で変更可。
   全帳票の発行元欄に営業所名・〒住所・TEL/FAX を印刷（officeBlockHtml）
5. **発注書の仕様変更**
   - 宛先に営業所名を含める／FAX番号を明記（施設が返送しやすいように）
   - **数量欄は空欄で印刷**（施設側が手書き記入する前提。入力すれば数字も出る）
   - **合計金額は出さない**（金額ONのときも単価列のみ。金額列・合計行なし）
   - 施設プルダウンを datalist 化し、**一覧にない新規施設名を直接入力**できる
6. **発注書・見積書の全項目を編集可能に**: 施設名・表題・日付・商品名・数量・単位・単価・備考・行の並べ替え・空行追加
7. **保存機能**（K.DOCS = taiyo_uriage_docs_v1）: 保存ボタンで保存し、「保存済み」から読み込んで再編集・上書き。
   JSONバックアップ・全削除の対象にも含まれる
8. **PDF保存**: jspdf + html2canvas を同梱（バンドル1.1MB）。印刷と同じHTMLを画面外に実寸描画してA4化。
   複数ページは自動でスライス。ファイル名は 発注書_施設名_日付.pdf

## 施設別画面とCSV出力の改善（2026-07-28 第3次指示）

1. **施設検索をオートコンプリート化**: 入力すると候補リスト（施設名＋今月売上、最大8件）が下に出て、
   クリックまたはEnterで選択。太枠のsky系にして「ここで探す」と分かる見た目に
2. **選んだら、その施設だけの画面に切り替わる**（一覧テーブルは非表示）。上部に「← 施設一覧へ戻る」。
   ダッシュボードのランキングからのジャンプも同じ単一施設ビューに入る
   - 内容: KPI5枚（今月売上・月平均・年間累計・粗利率・注文回数）／月別推移（大）／商品別売上TOP10／
     販売個数TOP10／分類別構成／最近の購入明細12件／各種ボタン（商品別CSV・販売リスト印刷・発注書・見積書）
3. **CSVの月見出しを日本語に**（`2025/8` → `2025年8月`）。Excelが「Aug-25」と日付変換してしまう問題への対処
4. **CSV出力に対象期間セレクタ**を追加: 全期間 ／ 今年度（4月始まり・fiscalStartIdx）／ 月を選ぶ（単月）
   - 列は 施設名・担当・直送・売上合計・粗利額・粗利率%（・月平均）＋期間内の月列
   - 単月のときは月平均列を出さない。ファイル名に期間が入る（例: 施設別売上_2026年度_20260728.csv）
   - 全期間対応のため buildAnalytics の施設に `byMonth: Map(monthIdx → {sales, gross})` を追加（12ヶ月窓に依存しない）
   - 単一施設ビューでは同じボタンがその施設だけを出力。別途「商品別CSV」（直近12ヶ月・商品×数量×売上×直近単価×最終購入日）も追加

## レポート4種と前年比較（2026-07-28 第4次指示）

1. **レポートを4種類に拡張**（ダッシュボードの「レポートを作る」から種類と対象期間を選ぶ）
   - 月次（選んだ1ヶ月）／年次（暦年1〜12月）／年度（4月〜翌3月）／月別推移（全期間を月ごとに並べる）
   - 印刷プレビューに加え **PDF保存** もこの場でできる
   - 帳票は `buildReportHtml` に共通化。月次以外は「月別の推移」表（月・純売上・粗利額・粗利率・取引施設数＋合計行）が入る
   - 集計は `buildPeriodReport(rows, masters, fromIdx, toIdx)` が明細を範囲で走査（12ヶ月窓に依存しない）
   - 検算済み: 年次2026=¥18,506,927 / 年度2026=¥11,318,333 / 全期間=¥33,493,231 / 月次2026年7月=¥2,781,184 が生データと一致
2. **TEL/FAXを別の行に**（officeBlockHtml・発注書ヘッダー・発注元欄すべて）。〒と住所も改行して折り返しをなくした
3. **増減率順の対象を限定**: `DECLINE_MIN_BASE = 50000`。今月0円、または直近3ヶ月平均が5万円未満の施設は
   増減率順のときだけ除外し、表の上に除外件数を表示（少額取引の％が上位を占める問題への対処）
4. **前年比較を追加**（担当別の表とダッシュボードのKPI）
   - 前月比（既存）／**前年同月比**（当月 vs 12ヶ月前）／**今年度累計**（4月〜基準月）／**前年度同期比**（その1年前の同期間）
   - 前年のデータが未取込の期間は「—」と表示し、注記で理由を説明する（誤った+100%を出さない）
   - buildAnalytics が facility.byMonth を使って全期間から算出。fiscalStartIdx は日付ユーティリティへ移動
   - ダミーの前年データを注入した検証で、前年同月比+175%・前年度同期比+307% が手計算と一致することを確認

## ダッシュボードのレポート表示を常設パネル化（2026-07-28 第5次指示・不具合修正）

「レポートを作る」という開閉式パネルをやめ、**期間選択バーをダッシュボード上部に常設**する形に作り替えた（DashboardView 全面書き換え）。3つの不具合を修正:

1. **PDF保存／印刷プレビューの位置が月次⇔年次でずれる**: 期間セレクタ（月/年/年度の3種類でDOM構造が違う）を `w-40` の固定幅コンテナに入れ、右側のボタン群を常に同じx座標に固定。実測で month/year/fy/trend の4種とも座標が完全一致することを確認
2. **印刷プレビューを閉じるとホーム画面に戻る**: 原因は「レポートを作る」ボタンで開閉する `repOpen` state があり、印刷実行時に `setRepOpen(false)` で閉じていたため、印刷プレビューを閉じるとレポートパネルも畳まれて素の（期間指定前の）ダッシュボードに見えていたこと。**repOpen state自体を廃止**し、期間選択とその結果（KPI・ランキング・分類別・推移グラフ）を常時表示にしたことで解消。印刷プレビューは他画面（施設別の単一施設ビュー等）を含め、開閉してもタブ・選択状態を一切変えない設計を徹底
3. **月次/年次/年度/月別推移を切り替えると下のデータも連動**: `buildPeriodReport(rows, masters, from, to)` を画面表示用にも使うよう変更。以前は「印刷用データだけ期間対応、画面はずっと基準月固定」だったのを、**KPIカード・施設別/商品別TOP10・分類別売上・月別推移グラフすべてが選択中の期間を参照**するように統一。前期比（前月/前年/前年度比）も期間種別ごとに自動で計算
   - 月次のときだけ「今年度累計」カードを追加表示（従来のダッシュボードKPIと整合）
   - 月別推移グラフは、月次選択時は「選んだ月までの12ヶ月」、それ以外は「選択期間そのもの」を表示

## CSV取込0件バグの修正（2026-07-28 第6次・重要）

**現象**: 「このCSVデータが読み込めませんでした」。ユーザーが用意した最新の売上照会CSVが1件も取り込めない不具合。

**原因**: 販売管理システムの出力仕様が変わり、以下2点が旧仕様と食い違っていた。
1. **売上日の年が4桁→2桁になった**（`2026/7/31` → `26/07/31`）。`DATE_RE` が `\d{4}` 固定だったため全行が
   「売上日が不正」で弾かれ、rowsParsed=0 → 「取り込める明細行がありません」で必ず失敗する状態だった
2. **担当者列名が「担当者」→「営業担当名」に変更**され、CSVに含まれる担当者情報が拾えなくなっていた
3. （参考）新たに「営業担当名」列が追加され、列数が23→24列になったが、列名マッピング方式のため実害なし

**対処**:
- `DATE_RE` を `(\d{2,4})` に緩め、2桁年は `yy<=68→20yy／yy>=69→19yy` の一般的なpivotで補正（parseDateStr内）
- 列名マッピングを「1キーにつき1列名」から**候補配列**に変更（`REQUIRED_COLS`/`OPTIONAL_COLS` の第2要素が配列に）。
  rep列は `['担当者','営業担当名','営業担当','担当']` を順に試す。表示用ラベルは `colLabel(names)`（先頭要素）
- 実CSV（6,179行、2025/4/1〜2026/7/31、新形式）で全行取込・担当者78施設自動割当・全画面表示・エラー0を確認

**教訓**: 販売管理システムの出力仕様（列名・日付書式）は予告なく変わりうる。今後もCSVが「0件」「読み込めない」場合は
まずプレビューモーダルの「不正な行の理由」（売上日が不正／列が見つからない等）を確認するのが最短の切り分け方。

## ダッシュボードのレポート種類を変更・施設名候補の位置バグ修正（2026-07-28 第7次指示）

1. **「年次」を廃止し「期間指定」を追加**。DashboardViewの repType は `month / fy / custom / trend` の4種に。
   `custom` は開始月・終了月の2つのプルダウンで自由に範囲を選べる（`customFrom`/`customTo` state）。
   前期比較は「選んだ期間と同じ長さの直前期間」で自動計算（例: 3ヶ月選べば直前3ヶ月と比較）
2. **施設名の候補ドロップダウンが左端など変な位置に出るバグを修正**。原因は DocComposer（発注書/見積書作成）の
   施設名入力に **ブラウザ標準の `<input list="...">` / `<datalist>`** を使っていたため。datalistの表示位置は
   ブラウザ実装依存でCSSから制御できず、埋め込みビュー環境では特に崩れやすい。
   **`<datalist>` を使わず、施設別検索と同じ自作の候補リスト**（絶対配置のdivをinputのすぐ下・同じ幅に出す、
   外側クリックで閉じる）に置き換えた。実測でinputとドロップダウンのx座標・幅が完全一致することを確認。
   → **今後、テキスト入力の候補提示には `<datalist>` を使わない**（施設別・DocComposer2箇所とも自作方式で統一）

## 適正在庫の設計（2026-07-27 指示）

余剰在庫を持ちすぎない目的で、当初案（最大月+余裕日数）から変更:

1. **必要在庫 = 月平均出荷 × 在庫回数（デフォルト1回分）**。最大月は参考表示。在庫回数は画面上部で変更可（settings.stockMonths に保存）
2. **商品ごとの扱い分類（手動）**: 在庫品／受注発注／直送品。適正在庫画面の行展開と設定→「在庫扱い」タブで変更。
   未分類は在庫品扱い（masters.itemHandling、'stock'はキー省略）
3. 施設ごとの直送フラグは従来どおり併存（直送施設への出荷は在庫需要から除外）
4. 画面は一覧＋CSV出力のみ（発注書エディタ・現在庫入力・単価記憶は 07-28 の方針変更で削除）

## 今後の拡張候補（未実装・要相談）

- 在庫回数の商品別個別設定 / 実在庫台帳
- 見積書の税区分3種（税抜/税込/非課税）切替、値引き行
- 保存した発注書・見積書の複製、施設ごとの絞り込み
- Firebase同期（firebase-office-sync スキル参照）

## 開発メモ

- ソース: `src/index.html`（text/babel 約3,100行）→ `cd build && node build.js` → `index.html`（約1.15MB）
- 印刷帳票ビルダー: buildMonthlyReportHtml / buildFacilitySalesHtml / buildFacilityOrderHtml / buildQuoteHtml
- 依存: react / react-dom / lucide-react / jspdf / html2canvas（すべて build/node_modules から同梱。CDN参照ゼロ）
- localStorageキー: rows_v1 / masters_v1 / meta_v1 / docs_v1
- プレビュー: launch.json `uriage-bunseki`（node _serve.cjs）
- 検証時の実CSV: `C:\Users\akama\OneDrive\デスクトップ\売上照会検索結果.csv`（Shift-JIS）
